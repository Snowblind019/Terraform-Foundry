# Declared with no value on purpose. Nothing sets it in this folder, so where
# the value comes from is decided at run time: the CLI prompt, a -var flag, or
# a TF_VAR_ environment variable.
variable "instance_type" {}
